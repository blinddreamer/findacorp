package com.drydock.profile.repository;

import com.drydock.profile.domain.PilotEnriched;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PilotEnrichedRepository extends JpaRepository<PilotEnriched, Long> {
}
