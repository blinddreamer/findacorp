package com.findacorp.profile.repository;

import com.findacorp.profile.domain.CorpEnriched;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CorpEnrichedRepository extends JpaRepository<CorpEnriched, Long> {
}
