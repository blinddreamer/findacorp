package com.drydock.application.domain;

public enum ParticipantSide {
    /** The pilot party of a thread. */
    PILOT,
    /** The corp party — expanded to the corp's CEO and appointed HR characters. */
    CORP
}
